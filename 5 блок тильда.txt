5 блок тильда
Хобби · HTML
<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Igor — Hobbies</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Science+Gothic:wght@400..900&family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
<style>
  :root{
    --bg:#0A0B0D;
    --panel:#13161B;
    --panel-2:#191D24;
    --line:#262B33;
    --text:#F4F6F9;
    --muted:#8C95A3;
    --blue:#20B5FF;
    --blue-dim:rgba(32,181,255,.12);
  }
  *{box-sizing:border-box;}
  html{scroll-behavior:smooth;}
  body{
    margin:0;
    background:var(--bg);
    color:var(--text);
    font-family:'Inter',sans-serif;
    -webkit-font-smoothing:antialiased;
  }
  h1,h2,h3{
    font-family:'Science Gothic','Inter',sans-serif;
    font-weight:800;
    letter-spacing:-0.01em;
    margin:0;
  }
  a{color:inherit !important; text-decoration:none !important;}
  .wrap{max-width:1200px; margin:0 auto; padding:0 32px;}
  section{padding:90px 0; scroll-margin-top:90px;}
  .eyebrow{
    font-family:'Inter',sans-serif;
    font-size:.78rem;
    letter-spacing:.18em;
    text-transform:uppercase;
    color:var(--blue);
    font-weight:600;
    margin-bottom:14px;
    display:block;
  }
 
  /* ---------- NAV ---------- */
  nav{
    display:flex; align-items:center; justify-content:space-between;
    padding:20px 32px;
    max-width:1200px; margin:0 auto;
    position:sticky; top:0; z-index:50;
    background:rgba(10,11,13,.85);
    backdrop-filter:blur(10px);
    border-bottom:1px solid var(--line);
  }
  .logo{
    font-family:'Science Gothic',sans-serif;
    font-weight:800;
    font-size:1.4rem;
    color:var(--blue) !important;
  }
  .nav-links{
    display:flex; gap:34px;
    list-style:none; margin:0; padding:0;
  }
  .nav-links a{
    font-size:.85rem;
    font-weight:600;
    letter-spacing:.04em;
    text-transform:uppercase;
    color:var(--text) !important;
    transition:color .2s ease;
    position:relative;
    padding-bottom:4px;
  }
  .nav-links a::after{
    content:'';
    position:absolute; left:0; bottom:0;
    width:0%; height:2px; background:var(--blue);
    transition:width .25s ease;
  }
  .nav-links a:hover{color:var(--blue) !important;}
  .nav-links a:hover::after{width:100%;}
  .nav-toggle{display:none;}
 
  /* ---------- HERO ---------- */
  .hero{
    display:grid;
    grid-template-columns:1.1fr 0.9fr;
    gap:48px;
    align-items:center;
    padding-top:70px;
    padding-bottom:60px;
  }
  .hero h1{
    font-size:clamp(2.4rem,5vw,3.6rem);
    line-height:1.05;
  }
  .hero h1 span{color:var(--blue);}
  .igor-name{
    display:inline-block;
    letter-spacing:0em;
    transition:letter-spacing .35s ease;
    cursor:default;
  }
  .igor-name:hover{letter-spacing:.18em;}
  .hero p.lead{
    color:var(--muted);
    font-size:1.02rem;
    line-height:1.7;
    max-width:46ch;
    margin-top:18px;
  }
  .stats-row{
    display:flex; gap:14px;
    margin-top:32px;
    flex-wrap:wrap;
  }
  .stat-box{
    background:var(--panel);
    border:1px solid var(--line);
    border-radius:12px;
    padding:16px 20px;
    min-width:110px;
  }
  .stat-box .num{
    font-family:'Science Gothic',sans-serif;
    font-weight:800;
    font-size:1.6rem;
    color:var(--blue);
  }
  .stat-box .label{
    font-size:.72rem;
    color:var(--muted);
    text-transform:uppercase;
    letter-spacing:.06em;
  }
  .btn-row{margin-top:34px; display:flex; gap:14px; flex-wrap:wrap;}
  .btn{
    display:inline-flex; align-items:center; gap:8px;
    padding:13px 24px;
    border-radius:999px;
    font-weight:600;
    font-size:.9rem;
    transition:transform .2s ease, background .2s ease, color .2s ease;
  }
  .btn-primary{background:var(--blue); color:#06131C !important;}
  .btn-primary:hover{transform:translateY(-2px); background:#4cc6ff;}
  .btn-outline{border:1.5px solid var(--line); color:var(--text) !important;}
  .btn-outline:hover{border-color:var(--blue); color:var(--blue) !important; transform:translateY(-2px);}
 
  /* hero card stage */
  .card-stage{
    position:relative;
    height:480px;
    display:flex; justify-content:center;
  }
  .card-stage canvas{width:100%; height:100%; touch-action:none;}
  .card-hint{
    position:absolute; bottom:6px; left:0; right:0;
    text-align:center; font-size:.74rem; color:var(--muted);
  }
 
  /* ---------- SECTION HEADERS ---------- */
  .sec-head{margin-bottom:42px;}
  .sec-head h2{font-size:clamp(1.7rem,3vw,2.3rem);}
 
  /* ---------- ABOUT ---------- */
  .about-grid{
    display:grid;
    grid-template-columns:1fr 1fr;
    gap:40px;
    align-items:start;
  }
  .about-grid p{color:var(--muted); line-height:1.75; font-size:1rem;}
  .info-card{
    background:var(--panel);
    border:1px solid var(--line);
    border-radius:14px;
    padding:20px 22px;
    margin-bottom:14px;
    transition:border-color .3s ease, transform .3s ease, box-shadow .3s ease;
  }
  .info-card:hover{
    border-color:var(--blue);
    transform:translateY(-4px);
    box-shadow:0 10px 26px rgba(32,181,255,.18);
  }
  .info-card .tag{font-size:.72rem; text-transform:uppercase; letter-spacing:.08em; color:var(--blue); font-weight:600;}
  .info-card .val{font-size:1.02rem; font-weight:600; margin-top:6px;}
 
  /* ---------- PATH A->B ---------- */
  .path-row{
    display:flex; align-items:center; gap:24px;
    flex-wrap:wrap;
    margin-top:10px;
  }
  .path-pill{
    flex:1; min-width:220px;
    background:var(--panel);
    border:1px solid var(--line);
    border-radius:14px;
    padding:22px;
    transition:border-color .3s ease, transform .3s ease, box-shadow .3s ease;
  }
  .path-pill:hover{
    border-color:var(--blue);
    transform:translateY(-4px);
    box-shadow:0 10px 26px rgba(32,181,255,.18);
  }
  .path-pill .pt{font-size:.72rem; color:var(--blue); text-transform:uppercase; letter-spacing:.1em; font-weight:700;}
  .path-pill p{color:var(--text); margin-top:10px; font-size:.96rem; line-height:1.55;}
  .path-arrow{font-size:1.8rem; color:var(--blue); flex-shrink:0;}
 
  /* ---------- MENTOR ---------- */
  .mentor-card{
    display:flex; gap:28px;
    background:var(--panel);
    border:1px solid var(--line);
    border-radius:18px;
    padding:28px;
    align-items:center;
    flex-wrap:wrap;
  }
  .mentor-photo{
    width:140px; height:140px;
    border-radius:50%;
    object-fit:cover;
    border:3px solid var(--blue);
    flex-shrink:0;
  }
  .mentor-card h3{font-size:1.25rem;}
  .mentor-card .role{color:var(--blue); font-size:.82rem; text-transform:uppercase; letter-spacing:.08em; font-weight:700; margin:4px 0 12px;}
  .mentor-card p{color:var(--muted); line-height:1.7; font-size:.96rem; max-width:60ch;}
 
  /* ---------- HOBBIES ---------- */
  .hobby-grid{
    display:grid;
    grid-template-columns:repeat(3,1fr);
    gap:16px;
  }
  .hobby-card{
    background:var(--panel);
    border:1px solid var(--line);
    border-radius:14px;
    padding:22px;
    text-align:center;
    transition:border-color .3s ease, transform .3s ease, box-shadow .3s ease;
  }
  .hobby-card:hover{
    border-color:var(--blue);
    transform:translateY(-6px) scale(1.03);
    box-shadow:0 10px 26px rgba(32,181,255,.2);
  }
  .hobby-card .ico{font-size:1.8rem; margin-bottom:10px;}
  .hobby-card h4{font-family:'Inter'; font-weight:600; font-size:.95rem; margin:0;}
 
  /* ---------- PROJECTS ---------- */
  .works-grid{
    display:grid;
    grid-template-columns:repeat(2,1fr);
    gap:18px;
  }
  .work-card{
    background:var(--panel);
    border:1px solid var(--line);
    border-radius:16px;
    padding:24px;
    transition:transform .25s ease, border-color .25s ease;
  }
  .work-card:hover{transform:translateY(-5px); border-color:var(--blue);}
  .work-card .num{color:var(--blue); font-family:'Science Gothic'; font-weight:800; font-size:1.2rem;}
  .work-card h4{margin-top:10px; font-size:1.05rem;}
  .work-card p{color:var(--muted); font-size:.88rem; margin-top:6px; line-height:1.5;}
 
  /* ---------- CONTACT / GITHUB ---------- */
  .contact-box{
    background:linear-gradient(135deg, var(--panel), var(--panel-2));
    border:1px solid var(--line);
    border-radius:20px;
    padding:48px;
    text-align:center;
  }
  .contact-box h2{font-size:clamp(1.6rem,3vw,2.1rem);}
 
  footer{
    text-align:center; padding:30px 0; color:var(--muted); font-size:.8rem;
    border-top:1px solid var(--line);
  }
 
  @media(max-width:860px){
    .hero{grid-template-columns:1fr;}
    .about-grid{grid-template-columns:1fr;}
    .hobby-grid{grid-template-columns:1fr 1fr;}
    .works-grid{grid-template-columns:1fr;}
    .card-stage{height:380px;}
  }
  @media(max-width:700px){
    nav{
      flex-wrap:wrap;
      gap:10px 0;
      padding:16px 20px;
    }
    .nav-links{
      width:100%;
      justify-content:center;
      gap:16px;
      flex-wrap:wrap;
      order:3;
    }
    .nav-links a{font-size:.74rem;}
  }
  @media(max-width:500px){
    .hobby-grid{grid-template-columns:1fr;}
    section{padding:60px 0;}
    .wrap{padding:0 20px;}
  }
  .stack{
    position:relative;
    height:230px;
    display:flex;
    align-items:center;
    justify-content:center;
  }
  .stack-card{
    position:absolute;
    width:280px; height:184px;
    background:var(--panel);
    border:1px solid var(--line);
    border-radius:10px;
    background-size:cover;
    background-position:top center;
    box-shadow:0 14px 30px rgba(0,0,0,.45);
    transition:transform .45s cubic-bezier(.2,.8,.2,1);
    transform:
      translateX(calc(var(--i) * 6px))
      rotate(calc(var(--i) * 3deg - 4deg));
  }
  .stack:hover .stack-card{
    transform:
      translateX(calc((var(--i) - 1.5) * 230px))
      rotate(0deg);
  }
  @media(max-width:700px){
    .stack{height:180px;}
    .stack-card{width:220px; height:145px;}
    .stack:hover .stack-card{
      transform: translateX(calc((var(--i) - 1.5) * 150px)) rotate(0deg);
    }
  }
 
</style>
</head>
<body>
 

<section id="hobbies">
  <div class="wrap">
    <div class="sec-head">
      <span class="eyebrow">Outside of work</span>
      <h2>Hobbies</h2>
    </div>
    <div class="hobby-grid">
      <div class="hobby-card"><div class="ico">🎮</div><h4>Video games</h4></div>
      <div class="hobby-card"><div class="ico">🎵</div><h4>Music</h4></div>
      <div class="hobby-card"><div class="ico">🏍️</div><h4>Motorcycles</h4></div>
    </div>
  </div>
</section>
 
 
</body>
</html>
 

